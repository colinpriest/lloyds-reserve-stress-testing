# Core dependencies
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=4.9.0

# PDF processing
pdfplumber>=0.10.0
PyMuPDF>=1.23.0

# Data processing
pandas>=2.0.0

# LLM APIs
openai>=1.0.0          # ChatGPT / GPT-5-mini for extraction & summarization
google-genai>=1.0.0    # Gemini for extraction
anthropic>=0.40.0      # Claude for adjudication (third-model tiebreaker)
# Note: Perplexity uses requests directly (no SDK needed)

# Optional: for async scraping (faster)
aiohttp>=3.9.0

# Rate limiting
ratelimit>=2.2.1

# For HTML to text conversion
html2text>=2020.1.16

# HTML to PDF conversion (for HTML syndicate reports)
playwright>=1.40.0

# Environment variable management
python-dotenv>=1.0.0

# OCR support for scanned PDFs
pytesseract>=0.3.10
pdf2image>=1.16.0
Pillow>=9.0.0

faiss-cpu
matplotlib
scipy
scikit-learn
sentence-transformers


# Streamlit interface
streamlit>=1.28.0

# Dash interface
dash-bootstrap-components

# Optional: for enhanced visualizations
plotly>=5.0.0
altair>=5.0.0

statsmodels

# Note: pdf2image requires poppler-utils to be installed on the system
# Windows: Download from https://github.com/oschwartz10612/poppler-windows/releases
# Linux: sudo apt-get install poppler-utils (or equivalent)
# macOS: brew install poppler
